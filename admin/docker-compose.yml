services:
  forex-admin-frontend:
    build: .
    container_name: forex-admin-frontend
    ports:
      - "127.0.0.1:80:80"
    restart: always
    networks:
      - web
networks:
  web:
    driver: bridge
    external: true
